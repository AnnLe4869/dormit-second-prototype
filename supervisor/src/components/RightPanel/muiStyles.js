import { createTheme } from "@mui/material";

export const sxButton = createTheme({
  palette: {
    primary: {
      main: "#7C91F4",
    },
    secondary: {
      main: "#EBEFFD",
    },
    text: {
      primary: "#7C91F4",
      secondary: "#EBEFFD",
    },
  },
  shape: {
    borderRadius: 20,
  },
});
