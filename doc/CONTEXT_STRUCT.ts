/**
 * -------------------------------------------------------------------------------------------------------------------------------------
 * the below are the structure for Context API - has nothing to do with Firestore
 * -------------------------------------------------------------------------------------------------------------------------------------
 */
type Alert = {
  type: "ERROR" | "INFO";
  message: string;
  is_active: boolean;
};
